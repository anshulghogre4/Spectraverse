# Core framework
fastapi>=0.111.0
uvicorn>=0.30.0          # drop [standard] — uvloop is Linux-only, not needed on Windows
python-multipart>=0.0.9
python-dotenv>=1.0.0
aiofiles>=23.2.1
pydantic>=2.9.0          # 2.9+ ships CP313 wheels — no Rust compilation needed
pydantic-settings>=2.5.0

# Image processing
Pillow>=10.4.0

# Audio processing
librosa>=0.10.2          # 0.10.2+ relaxes numba constraint — compatible with Python 3.13
numpy>=1.26.0,<3.0
scipy>=1.13.0

# Visualisation (spectrogram generation)
matplotlib>=3.9.0

# Spectrogram inversion (Sprint 3)
opencv-python-headless>=4.9.0  # headless — no GUI deps needed on server
soundfile>=0.12.0               # read/write WAV without scipy limitation

# Cloud & caching
azure-storage-blob>=12.22.0
redis>=5.0.0

# Sprint 3 — Foundry IQ + GPT-4o vision (optional, app works without these via mock fallback)
# Install with: pip install --pre azure-search-documents
azure-search-documents>=11.7.0b2  # Pre-release — needed for 2026-05-01-preview KnowledgeBase APIs
azure-identity>=1.17.0             # DefaultAzureCredential for keyless auth
openai>=1.55.0                     # Azure OpenAI client for GPT-4o vision calls
